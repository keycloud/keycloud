import { UsernameMasterPassword } from './username-master-password';

describe('UsernameMasterPassword', () => {
  it('should create an instance', () => {
    expect(new UsernameMasterPassword()).toBeTruthy();
  });
});
